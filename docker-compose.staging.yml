name: stokku-staging

include:
  - docker-compose.yml

services:
  api:
    ports:
      - "${API_PORT:-4001}:3001"
    environment:
      NODE_ENV: staging

  web:
    ports:
      - "${WEB_PORT:-3001}:3002"
    environment:
      NODE_ENV: staging
